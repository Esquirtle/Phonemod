package esq.phonemod.device.events;

/**
 * Marker interface for all device/phone framework events.
 *
 * <p>Subscribe to concrete event types via {@link PhoneEvents#subscribe}.
 * Post events via {@link PhoneEvents#post}.
 */
public interface DeviceEvent {
}
